# Application
BASE_URL=https://dev.bidbuddy.com
DEFAULT_TIMEOUT_MS=30000

# Browser
HEADLESS=true
SLOW_MO_MS=0

# Test credentials — set locally in .env or in GitHub Actions secrets (never commit real values)
PM_EMAIL=your-pm-email@example.com
PM_PASSWORD=your-pm-password
VENDOR_EMAIL=your-vendor-email@example.com
VENDOR_PASSWORD=your-vendor-password
